.row
  = form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
    = devise_error_messages!
    .col-md-6
      .form-group
        = label_tag(:first_name, "Your first name *")
        = f.text_field :first_name, :placeholder => 'First name', :class => 'form-control'
      .form-group
        = label_tag(:last_name, "Your last name")
        = f.text_field :last_name, :placeholder => 'Last name', :class => 'form-control'
      - # Editing password and email for existing users is disabled for now.
      - # Feel free to activate it, but propably the RegistrationsController needs
      - # to be adapted to make it work.
      - if resource.new_record?
        .form-group
          = label_tag(:email, "Your email address *")
          %small (don't worry, your email won't be displayed publicly)
          %br/
          = f.email_field :email, :autofocus => true, :placeholder => 'E-mail', :class => 'form-control'
        .form-group
          = label_tag(:password, "Enter your password *")
          = f.password_field :password, :placeholder => 'Password',:class => 'form-control'
        .form-group
          = label_tag(:password_confirmation, "Confirm your password *")
          = f.password_field :password_confirmation, :placeholder => 'Password Confirmation', :class => 'form-control'
      - else
        .form-group
          = label_tag(:working_on, "What are you working on? (you can use markdown!)")
          = f.text_area :working_on, placeholder: 'tutorials? a cool app?', class: 'form-control', rows: 5, id: 'working-on'
    .col-md-6
      .form-group
        = label_tag(:twitter, "Your twitter handle")
        = f.text_field :twitter, :placeholder => '@twitterhandle', :class => 'form-control'
      .form-group
        = f.check_box(:willing_to_coach)
        = label_tag(:person_willing_to_coach, "I am willing to coach")
      .form-group
        = label_tag(:picture, "Add your profile picture")
        = f.file_field :picture, :placeholder => 'picture'
        %p.help-block Choose your profile picture
      - if resource.new_record?
        .form-group
          = f.submit "Sign up", :class => 'btn btn-info'
        = render "devise/shared/links"
      - else
        .form-group
          = f.submit "Save", :class => 'btn btn-info'
