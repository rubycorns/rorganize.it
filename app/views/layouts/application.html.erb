<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RailsGirlsApp</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header class="header-container">
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to("RailsGirlsApp", root_path, {class: "navbar-brand"}) %>
        </div>
        <div class="collapse navbar-collapse navbar-ex1-collapse pull-left">
          <ul class="nav navbar-nav">
            <li class="<%= 'active' if params[:controller] == 'groups' %>">
              <%= link_to "Groups", groups_path %>
            </li>
            <li class="<%= 'active' if params[:controller] == 'students' %>">
              <%= link_to "Students", students_path %>
            </li>
            <li class="<%= 'active' if params[:controller] == 'coaches' %>">
              <%= link_to "Coaches", coaches_path %>
            </li>
          </ul>
        </div>
        <div>
          <% if person_signed_in? %>
            <div class="media pull-right profile-header">
              <a class="pull-left" href="#">
                <%= image_tag(current_person.picture_url, size: "32x32" ) if current_person.picture.present? %>
              </a>
              <div class="media-body">
                  <p class="media-heading"><%= current_person.full_name %><br>
                  <%= link_to "logout", destroy_person_session_path, method: :delete %></p>
                </div>
            </div>
          <% end %>
        </div>
      </div>
    </nav>
  </header>

  <div class="main-container">
    <% if alert %>
      <p class="container alert alert-info alert-dismissable"><%= alert %><button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button></p>
    <% end %>
    <%= yield %>

  </div>

  <footer class="footer-container">
    <div class="container">
      <p class="pull-left">made with <span class="glyphicon glyphicon-heart"></span> by the <strong><%= link_to("rubycorns", "https://twitter.com/rubycorns") %></strong></p>
      <ul class="inline pull-right">
        <li><a href="https://github.com/KaMaria/RailsGirlsApp">Code on Github</a></li>
        <li><a href="http://huboard.com/KaMaria/RailsGirlsApp/board">Collab on Huboard</a></li>
      </ul>
    </div>
  </footer>
</body>
</html>
