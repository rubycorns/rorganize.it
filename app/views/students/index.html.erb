<div class="container student-list">
  <h1>Students overview</h1><br /><br />
  <div class="row">
    <div class="col-md-8">
      <ul class="list-group">
        <% @students.each do |student| %>
          <li class="list-group-item clearfix">
            <div class="media pull-left">
              <a class="pull-left" href="<%= student_path(student) %>">
                <%= image_tag(student.picture_url(:thumb), size: "40x40") if student.picture.present? %>
              </a>
              <div class="media-body">
                <h4 class="media-heading"><%= student.full_name %></h4>
                  <%= render partial: "shared/groups", locals: { object: student } %>
              </div>
            </div>
              <div class="pull-right">
                <div class="btn-group">
                  <%= link_to student_path(student), :class => 'btn btn-default' do %>
                    <%= fa_icon "user", text: "Profile" %>
                  <% end %>
                </div>
              </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>