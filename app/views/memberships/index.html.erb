<div class='container'>
  <h2 class='page-header'>These people want in!</h2>
  <div class='row'>
    <div class='col-md-8'>
      <table class='table table-striped'>
        <thead>
          <tr>
            <th>Name</th>
            <th>Type</th>
            <th></th>
            <td></td>
          </tr>
        </thead>
        <tbody>
          <% @pending.each do |pending| %>
            <tr>
              <td><%= pending.person.full_name %></td>
              <td><%= pending.name %></td>
              <td><%= link_to 'accept', memberships_path(pending), method: :put %></td>
              <td><%= link_to 'deny' %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
