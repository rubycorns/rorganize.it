= render 'subnav'

.flex-index
  .flex-index-main
    %h1.page-header My Groups
    %p Groups I belong to.

    - if @my_groups.present?
      %ul.list-group
        = render partial: 'one_group', collection: @my_groups, as: :group
    - else
      %p Nothing here yet

  = render 'sidebar'
