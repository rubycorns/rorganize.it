%p
  %strong 🗓️ When
  - if group.time?
    = group.time
  - else
    %span.info-not-given
      not specified
%p
  %strong 🏢 Meeting place
  - if group.address?
    = group.address
  - else
    %span.info-not-given
      not specified
%p
  %strong 🗺️ Address
  = group_location(group)
