= render 'subnav'

.flex-index
  .flex-index-main
    %h1.page-header Active groups
    %p These groups are actively meeting.

    - if @groups.present?
      %ul.list-group
        = render partial: 'one_group', collection: @groups, as: :group
    - else
      %p Nothing here yet

  = render 'sidebar'
