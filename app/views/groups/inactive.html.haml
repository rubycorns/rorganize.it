= render 'subnav'

.flex-index
  .flex-index-main
    %h1.page-header Inactive groups
    %p These groups do no longer exist or meet.

    - if @groups.present?
      %ul.list-group
        = render partial: 'one_group', collection: @groups, as: :group
    - else
      %p Nothing here yet

  = render 'sidebar'
