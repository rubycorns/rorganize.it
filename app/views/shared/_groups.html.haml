.people--groups
  %p
    %strong Groups:
  %ul.people--groups-groups
    - if object.groups.any?
      - object.groups.each do |group|
        %li= link_to group.name, group
    - else
      %li.info-not-given not specified
