%ul.sub-nav{role: "navigation"}
  %li= link_to "← All posts", posts_path

%h1 Editing post
.action-buttons
  // TODO: check the functionality of Show
  = link_to 'Cancel', posts_path, class: 'btn btn-outline'

= render 'form'
